node_modules/
_*.jpg
_*.png
*.b64
__pycache__/

# herramientas de desarrollo: no son parte de la web
.agents/
.claude/
# …salvo los permisos, que sí conviene que viajen con el repositorio:
# sin esto se quedan en el contenedor y hay que volver a darlos cada vez.
!.claude/settings.json
skills-lock.json

# la copia con la piel de diseño: se genera para enseñarla y se sirve
# desde el directorio de trabajo, NUNCA forma parte del portal
adolphus-pro.html
